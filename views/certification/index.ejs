<%- contentFor('content') %>
<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-6"><h5>Certification</h5></div>
            <div class="col-6 text-end"><a href="/certification/create" class="btn btn-primary"> Create </a></div>
        </div>
    </div>
    <div class="table-responsive text-nowrap">
        <table class="table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Year</th>
                    <th>Institution</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                <% certification.forEach(e => { %>
                    <tr>
                        <td><%= e.title %> </td>
                        <td><%= e.description %> </td>
                        <td><%= e.year %> </td>
                        <td><%= e.institute %> </td>
                        <td>
                            <span class="badge bg-label-<%= e.status?'success':'danger' %>  me-1"><%= e.status?'Active':'Pause' %></span>
                        </td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="certification/edit/<%= e.id %>"><i class="bx bx-edit-alt me-1"></i>
                                        Edit</a>
                                    <a class="dropdown-item" href="certification/delete/<%= e.id %>"><i class="bx bx-trash me-1"></i>
                                        Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                <% }) %>
                
              
            </tbody>
        </table>
    </div>
</div>